export const Switch = ({ defaultChecked }) => (
    <input
      type="checkbox"
      defaultChecked={defaultChecked}
      className="h-5 w-5 text-teal-600 rounded focus:ring-0"
    />
  );
  